@model Producciones.Models.ArticulosProduccion

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Create</h1>

<h4>ArticulosProduccion</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            <div class="form-group">
                <label asp-for="Produccion" class="control-label"></label>
                <select asp-for="Produccion" class ="form-control" asp-items="ViewBag.ProduccionId"></select>
            </div>
            @* <div class="form-floating mt-1">
                <select asp-for="ProcesoId" class="form-select d-block" asp-items="ViewBag.Proceso">
                    <option selected></option>
                </select>
                <label class="d-block">Seleccione un proceso</label>
            </div> *@
            <div class="form-group">
                <label asp-for="CodGenArt" class="control-label"></label>
                <select asp-for="CodGenArt" class="form-control" asp-items="ViewBag.Articulo"></select>
            </div> 
            <div class="form-group">
                <input type="submit" value="Agregar" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-danger">Listo</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
